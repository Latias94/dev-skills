interface:
  display_name: "Handoff"
  short_description: "Compact a conversation into a handoff"
  default_prompt: "Use $handoff to turn this conversation into a compact handoff for another agent."
policy:
  allow_implicit_invocation: false
