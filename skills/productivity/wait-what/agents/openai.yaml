interface:
  display_name: "Wait What"
  short_description: "Re-pitch that — simpler, with the context I'm missing"
  default_prompt: "Use $wait-what to re-pitch the last response in the current conversation's language with the missing context."
policy:
  allow_implicit_invocation: false
