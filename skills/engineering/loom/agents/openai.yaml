interface:
  display_name: "Loom"
  short_description: "Discover safe parallel work lanes"
  default_prompt: "Use $loom to inspect this repo goal, discover safe parallel lanes, assign file ownership, and coordinate review-gated implementation."
