interface:
  display_name: "Open Workstream"
  short_description: "Create or reuse a Rust workstream"
  default_prompt: "Use $open-workstream to create a durable Rust workstream and split vertical tasks."
policy:
  allow_implicit_invocation: true
