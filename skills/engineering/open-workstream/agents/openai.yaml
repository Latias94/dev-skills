interface:
  display_name: "Open Workstream"
  short_description: "Create or reuse a Rust workstream"
  default_prompt: "使用 $open-workstream 创建 durable Rust workstream，并拆分垂直任务。"
policy:
  allow_implicit_invocation: true
