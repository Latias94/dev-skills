interface:
  display_name: "Codex Session Recovery"
  short_description: "Import prior session and subagent continuity"
  default_prompt: "Use $codex-session-recovery once to import the active goal, persisted subagent lineage, and safe continuation context from a previous Codex session. Treat the receiving or resumed session and its later compactions as authoritative."
