interface:
  display_name: "Engineering Wiki Memory"
  short_description: "Record immutable repo-local engineering memory"
  default_prompt: "Use $engineering-wiki-memory to record immutable OKF-compatible engineering-memory shards for parallel lanes, progress, decisions, handoffs, findings, blockers, next actions, and verification; leave rollup rendering to integration."
