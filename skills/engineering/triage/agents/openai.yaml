interface:
  display_name: "Triage"
  short_description: "Move issues through triage roles"
  default_prompt: "Use $triage to turn these issues into verified, agent-ready work."
policy:
  allow_implicit_invocation: false
