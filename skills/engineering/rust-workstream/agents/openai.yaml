interface:
  display_name: "Rust Workstream"
  short_description: "Plan and run large Rust workstreams"
  default_prompt: "Use $rust-workstream to plan this large Rust change, split vertical tasks, and coordinate agent execution."
policy:
  allow_implicit_invocation: true
