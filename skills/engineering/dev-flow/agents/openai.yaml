interface:
  display_name: "Dev Flow"
  short_description: "Route large Rust development workflow"
  default_prompt: "Use $dev-flow to decide which skill and workflow phase should handle this Rust project task."
policy:
  allow_implicit_invocation: true
